//Condition ?  Expression one : expression two
let numberNumber = 2;
let result = numberNumber % 2 === 0 ? 'even' : 'odd';
console.log(result);

 let time = 13;
 let dayTime = time < 12 ? 'Good morning' : time < 18 ? 'Good afternoon' : 'Good evening';
 console.log(dayTime);

 let isLoggedIn = false;
 let checkLog = isLoggedIn ? 'Welcome Back!' :  'Access denied. Please log in';
console.log(checkLog);
 